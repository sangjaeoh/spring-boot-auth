# 인증 서비스 요구사항 (Requirements)

Spring Boot 기반 통합 인증 서비스. 국내 **대외(공개) 서비스**에 제공할 회원 인증·계정·세션 관리 기반 시스템의 요구사항(*무엇을 만드는가*)을 정의한다. 실제 구현 모델(엔티티·필드·정책·불변식)은 [`DOMAIN_MODEL.md`](./DOMAIN_MODEL.md)가 소유한다.

- **대상 사용자**: 일반 회원 · 서비스 운영 관리자
- **기준 방향**: JWT(Access) + **서버 세션 저장소(Redis)** — 강제 로그아웃·다중 로그인 제한을 실시간 반영(의도적 stateful). 세션=진실원본, 이력/감사=RDB. 외부 연동(소셜·본인인증·알림)은 포트로 격리해 dev는 Mock로 대체.

## 범위

- **포함**: 로컬 인증(가입·로그인·로그아웃·비밀번호), 소셜 4종·연동, 토큰/세션 관리, 디바이스·다중 로그인 제한·강제 로그아웃, 본인인증·약관/동의, 이상탐지·알림, 레이트리밋·계정 잠금, RBAC·관리자 콘솔·감사, 회원 탈퇴.
- **제외**: MFA/2단계 인증(구조 슬롯만 확보), 결제·정산, 서비스 도메인 고유 로직, 프론트엔드 화면.

## 용어

| 용어 | 정의 |
|---|---|
| Access Token | 짧은 수명의 API 접근 토큰(JWT), `sid`(세션ID) 클레임 보유 |
| Refresh Token | Access 재발급용 장수명 토큰(회전·재사용 탐지 대상) |
| 세션 | 사용자-기기 단위 로그인 상태(서버 저장소가 진실원본) |
| 본인인증 | 외부 본인확인기관을 통한 실명확인(CI/DI 획득) |
| CI / DI | 연계정보(CI=사람 단위, 중복가입 방지) / 사이트별 식별(DI) |
| 강제 로그아웃 | 서버가 특정/전체 세션을 즉시 무효화 |
| 다중 로그인 제한 | 사용자당 동시 활성 세션 수 상한 |

## 기능 요구사항

### 인증 (로그인·세션·토큰)

- 이메일+비밀번호 로그인을 제공하고 성공 시 Access/Refresh 토큰을 발급한다.
- 로그아웃 시 현재 세션을 즉시 무효화한다.
- JWT(Access)는 짧은 수명을 가지며, 매 요청 시 서버 세션 유효성을 검증한다(무효 세션 즉시 거부).
- Refresh 토큰 회전(rotation)을 적용한다.
- 이미 사용/폐기된 Refresh 재사용을 탈취로 감지해 해당 세션 전체를 무효화하고 사용자에게 알린다.
- 사용자당 동시 활성 세션 상한(기본 3)을 적용하고, 초과 시 가장 오래된 세션을 자동 종료·알림한다.
- 내 활성 세션 목록(기기·IP·최근 접속)을 조회한다.
- 특정 세션 또는 현재 제외 전체 세션을 사용자가 원격 로그아웃한다.
- 관리자가 특정 회원의 전체 세션을 강제 종료한다(남은 토큰도 즉시 거부).
- 로그인 성공/실패·로그아웃을 이력으로 기록한다.

### 회원 (계정·생명주기)

- 이메일 기반 회원가입을 제공하고, 가입 시 이메일 인증·휴대폰 인증을 요구한다.
- 가입은 본인인증 완료와 필수 약관 동의를 전제로 한다.
- 회원 상태를 두 개의 독립 축으로 관리한다: 생명주기(활성/휴면/탈퇴) + 계정 잠금 오버레이(잠금없음/일시잠금/관리자잠금). 화면에는 두 축을 합성한 표시 상태(활성/휴면/잠금/탈퇴)로 노출한다.
- 내 정보 조회·수정을 제공한다.
- 휴면: 1년(12개월) 미접속 시 휴면 전환, 30일 전 사전통지, 로그인 후 재인증(OTP)으로 해제, 휴면 데이터 분리보관(법적 의무 아닌 서비스 정책, 기간 조정 가능).
- 탈퇴: PII를 즉시 파기(법정 보존 항목 제외)하고, 부정 재가입 방지로 CI 해시 등 최소 식별정보만 유한 보존한다. 재가입은 이전 이력을 승계하지 않는 새 계정으로 처리하며, 동일인(CI)·이메일·전화 기준 쿨다운(기본 30일)을 적용한다.

### 본인인증

- 외부 본인확인기관 연동으로 실명확인을 수행한다(PASS/아이핀 수준).
- 결과로 이름·생년월일·성별·통신사·휴대폰·CI/DI를 확보한다.
- CI를 중복가입 방지 키로 사용한다(동일인 다중가입 차단).
- 개인정보는 최소 수집·암호화 저장·보존기간을 준수한다.
- 개발/테스트 환경은 외부 호출 없는 Mock 인증을 제공한다.

### 약관·동의

- 약관을 유형·버전으로 관리한다(이용약관, 개인정보 필수/선택, 만14세, 마케팅, 제3자 제공 등).
- 가입·이용 중 동의/철회를 처리한다.
- 동의·철회 이력을 append-only로 영구 보관한다.
- 선택 동의는 회원이 직접 변경한다.

### 소셜 로그인

- 카카오·네이버·구글·애플 OAuth2/OIDC 로그인을 제공한다.
- 소셜 최초 로그인 시 약관동의·본인인증 온보딩으로 유도한다.
- 로그인 상태에서 소셜 계정 연동/해제를 제공한다(최소 1개 로그인 수단 유지).
- 애플 특수사항(동적 client_secret, id_token 검증, 최초 1회 정보, 릴레이 이메일)을 처리한다.

### 디바이스

- 로그인 시 기기 정보를 등록·식별한다(기기명·플랫폼·최근 IP/시각).
- 내 등록 기기 목록을 조회한다.
- 신뢰 기기 등록/해제를 제공한다.
- 기기 삭제 시 해당 기기의 세션을 종료한다.
- 푸시 토큰·OS 권한을 관리한다.

### 비밀번호

- 비밀번호 재설정(메일/토큰) 및 로그인 상태 변경을 제공한다.
- 비밀번호 정책(길이·복잡도)과 최근 N개 재사용 금지를 적용한다.
- 안전한 해시(Argon2id/BCrypt)로 저장한다.

### 이상탐지·알림

- 신규 기기/신규 지역 로그인을 감지한다.
- 이상 로그인·신규 기기·비밀번호 변경 시 사용자에게 알림(이메일/SMS/PUSH)을 발송한다.
- 로그인 위험도(risk score)를 산출해 이력에 남긴다.
- 알림 수신 설정(채널×카테고리 opt-in)을 제공한다. 보안 카테고리는 수신거부 불가다.

### 보안·레이트리밋

- 로그인·인증코드 발송 등 민감 엔드포인트에 IP/계정 기준 요청 제한을 적용한다.
- 로그인 연속 실패 임계 초과 시 계정을 일시 잠금한다.
- 인증코드(SMS/이메일) 일일 발송 한도를 적용한다.

### 권한·관리자

- RBAC: 역할(일반/관리자/최고관리자)·권한을 관리하고, 관리자 API에 역할·권한 기반 접근 통제를 적용한다.
- 관리자 콘솔: 회원 검색/상세(민감정보 마스킹), 강제 로그아웃, 계정 잠금/해제, 로그인 이력 조회, 권한 변경.

### 감사

- 로그인/로그아웃/토큰 재발급/강제 로그아웃 등 인증 이벤트를 기록한다.
- 관리자 행위·권한 변경을 변경 전/후 값과 함께 append-only로 기록한다.
- 감사 로그는 수정 불가하며 조회를 제공한다.

## 비기능 요구사항

- **보안**: 전 구간 HTTPS, 토큰 서명·검증, 개인정보 암호화 저장, 민감정보 마스킹, 위조/만료/도용 방어, CSRF/CORS.
- **컴플라이언스**: 개인정보보호법 준수 — 동의 이력 보관, 수집 최소화, 보존/파기, 접근 감사.
- **성능**: 세션 검증 오버헤드 최소화(요청당 세션 저장소 조회 1회 수준), 인증 API 저지연.
- **확장성**: 무상태 애플리케이션 + 외부 세션 저장소로 수평 확장. 외부 연동은 인터페이스로 격리해 교체 가능.
- **가용성/운영**: 헬스체크·모니터링·구조적 로깅, 알림/외부연동 실패 시 재시도·격리.
- **테스트 용이성**: 외부 연동(소셜·본인인증·알림)을 Mock으로 대체해 오프라인 E2E 검증.

## 외부 연동

- **소셜 로그인**: 카카오·네이버·구글·애플 OAuth2/OIDC (각 사 개발자앱·키, 애플은 `.p8`).
- **본인확인기관**: NICE/KG모빌리언스/다날 등 PASS·아이핀 (계약·사이트코드, dev는 Mock).
- **알림 발송**: 이메일(SMTP/SES 등)·SMS(발송대행)·PUSH(FCM/APNs) (dev는 Mock).

## 단계별 릴리스

| 단계 | 내용 |
|---|---|
| Phase 1 | 코어 로컬 인증(가입·로그인·JWT·세션·비번) — 독립 배포 MVP |
| Phase 2 | 소셜 로그인 4종 + 연동/해제 |
| Phase 3 | 디바이스 + 다중 로그인 제한 + 강제/원격 로그아웃 |
| Phase 4 | 본인인증 + 약관·동의 + 회원 탈퇴 |
| Phase 5 | 이상탐지·알림 + 레이트리밋·계정 잠금 |
| Phase 6 | 관리자 콘솔 + RBAC + 감사 로그 |
| Phase 7(옵션) | MFA/2단계 인증 |

## 제약·전제

- 본인확인기관·소셜 각 사와의 실계약/키 발급이 프로덕션 배포 전 완료되어야 한다(개발은 Mock으로 선행 가능).
- 세션 저장소(Redis)는 핵심 의존이며 고가용 구성이 필요하다.
- 개인정보 처리방침·약관 원문 및 보존기간 정책은 법무/기획에서 확정·제공한다.

## 향후 확장

- MFA/2단계 인증(TOTP·SMS OTP·백업코드) — 본 범위에서는 데이터 모델 슬롯만 확보.
- 소셜 provider 추가, 위험 기반 인증(리스크 기반 추가인증) 고도화.
